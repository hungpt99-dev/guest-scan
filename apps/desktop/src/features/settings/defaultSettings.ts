import type { AppSettings } from "./settingsTypes";

export const DEFAULT_SETTINGS: AppSettings = {
  ocrWorkerPath: "",
  ocrLanguage: "eng",
  outputDirectory: "",
  tempDirectory: "",
  theme: "light",
};
